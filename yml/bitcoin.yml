bitcoin:
  dir: bitcoin
  enabled: true
  url: https://github.com/bitcoin/bitcoin.git
  branch: master
  clean:
    - make distclean
  build:
    - ./autogen.sh && \
    - ./configure --with-incompatible-bdb --disable-wallet && \
    - make -j$__PROCS__
